AWS_REGION=us-east-1
S3_BUCKET=mi-bucket
S3_PREFIX=PDA/2026_01/
LOCAL_DIR=data
FILE_PATTERN=*.pdf
MANIFEST_PATH=outputs/manifest.json

MAX_WORKERS=8
MULTIPART_THRESHOLD_MB=64
MULTIPART_CHUNKSIZE_MB=16

# Recomendado: IAM role / AWS SSO / perfil (~/.aws/credentials)
