# Seguridad

⚠️ **No subas a GitHub**:
- `.env` (credenciales)
- `data/` (datasets reales)
- `outputs/` (predicciones y modelos entrenados)
- Cualquier archivo con PII

El notebook original tenía credenciales hardcodeadas; aquí se reemplazaron por variables de entorno.
