# Seguridad (importante)

**NO** subas a GitHub:
- `data/` (dataset real)
- `outputs/` (modelos entrenados, predicciones)
- `.env` (credenciales)
- archivos con PII (IDs, correos, teléfonos)

Si algún notebook tuvo credenciales hardcodeadas, muévelas a `.env` y rota la clave.
